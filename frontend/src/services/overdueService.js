/**
 * Overdue bucket summary — replace with API when available.
 * @returns {Promise<{ ok: boolean, items: object[], error: string | null }>}
 */
export async function fetchOverdueBuckets() {
  return { ok: true, items: [], error: null }
}
