/**
 * Workflow template steps and comments — replace with API calls when backend endpoints exist.
 * @returns {Promise<{ ok: boolean, items: object[], error: string | null }>}
 */
export async function fetchWorkflowSteps() {
  return { ok: true, items: [], error: null }
}

/**
 * @returns {Promise<{ ok: boolean, items: object[], error: string | null }>}
 */
export async function fetchApprovalComments() {
  return { ok: true, items: [], error: null }
}
